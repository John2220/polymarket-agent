# Сводка по ставкам проекта Polymarket Agent

*Секции 1–3: локальная БД `agent.db` (`python scripts/print_bets_summary.py`). Секция 4: учёт в Excel — `python refresh.py`.*

## 1. Агрегаты

### Реальные ставки (`bets`)

| Показатель | Значение |
|------------|----------|
| Всего записей | 1 |
| Завершённых (есть win/loss в логике БД) | 0 |
| Сумма задействованных средств (size_usd) | $10.00 |
| Суммарный P&L по разрешённым | $0.00 *(нет разрешённых)* |

### Forward-test (`snapshots`)

| Показатель | Значение |
|------------|----------|
| Всего записей | 1 |
| Завершённых | 0 |
| Ожидают исхода | 1 |
| Виртуальный оборот (рекоменд.) | $10.00 |
| Виртуальный P&L | $0.00 *(не закрыто)* |

### `daily_performance`

Пусто (дневная агрегация не заполнялась).

---

## 2. Все строки: `bets`

| id | Рынок (кратко) | Сторона | Цена | $ | Edge | Режим | Статус | P&L | Разрешено |
|----|----------------|---------|------|---|------|--------|--------|-----|-----------|
| 1 | Will Aston Villa FC win on 2026-02-27? | YES | 0.48 | 10 | 0.05 | recommend | recommended | — | нет |

`market_condition_id`: `aston_villa_2026_02_27` (тестовый id; для авто-резолва в Gamma нужен реальный condition_id).

---

## 3. Все строки: `snapshots` (forward-test)

| id | Рынок | Лига | Сторона | PM | Pinnacle p* | Edge | Реком. $ | Результат |
|----|-------|------|---------|-----|-------------|------|----------|-----------|
| 1 | Will Aston Villa FC win on 2026-02-27? | soccer_epl | YES | 0.48 | 0.53 | 0.05 | 10 | ожидание |

---

## 4. Excel (основной учёт реальных ставок)

Файл: **`polymarket_рекомендации.xlsx`** в корне `polymarket-agent`. Обновление строк «Ставки» / «Статистика»: `python refresh.py` (ручные исходы — `MANUAL_RESULTS` в `refresh.py`).

### Агрегаты по Excel — 2026-03-21

| Показатель | Значение |
|------------|----------|
| Закрытых | 9 |
| Выигрышей / проигрышей | 2 / 7 |
| Win Rate | 22.2% |
| Итого P&L (закрытые) | **+$7.22** |
| Активных | 10 |

Последняя проверка: **Game Handicap: LY (−2.5) vs LOUD (+2.5)** — Gamma API `events?slug=lol-ly-lll-2026-03-17`, рынок `lol-ly-lll-2026-03-17-game-handicap-away-2pt5`: `outcomePrices` [0,1] на `["LYON","LOUD"]` → LOUD; YES (LYON) — проигрыш, P&L по строке из `refresh.py`.

---

## 5. Команды для обновления сводки

```bash
python refresh.py                # Excel + MANUAL_RESULTS → статистика листа
python check_results.py          # подтянуть исходы из Gamma → БД
python main.py --mode stats      # статистика в консоли
python scripts/print_bets_summary.py   # эта таблица (только SQLite)
```
